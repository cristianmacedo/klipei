import type { Metadata } from "next";
import { Inter } from "next/font/google";
import { Toaster } from "@/components/ui/sonner";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Klipei - Content Rewards Platform",
    template: "%s | Klipei",
  },
  description:
    "Conecta marcas a clippers que divulgam seu conteúdo e ganham por visualização. Pague apenas por views reais.",
  keywords: ["content rewards", "clipping", "marketing", "views", "creators"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
        <Toaster richColors position="top-right" />
      </body>
    </html>
  );
}
